# Отчёт за 2026-05-13

## 18:00 — Реализован UI MVP-демки конфигуратора

- Создан UI-кит (Button/Card/Badge/Input/Select/Textarea/Modal/Table/Tabs/Progress/Toast/EmptyState/Tooltip/Avatar/IconButton + 25 inline SVG-иконок) на CSS Modules
- Sidebar 240px + Header 60px + Shell-обёртка с хлебными крошками и Toast-контейнером
- 4 Zustand-стора с persist в localStorage (clients/projects/settings/toast)
- Дашборд `/` с 4 KPI, последними проектами, топ-клиентами и CTA
- Страницы клиентов: список с фильтрами, карточка, форма (RHF+Zod) с валидацией ИНН и предупреждением о дубликате
- Страницы проектов: список с фильтрами, мастер создания (3 шага), карточка с drop-zone ТЗ, списком систем, сводкой и удалением
- AI-парсинг с мок-прогрессбаром (3.5 сек, 4 стадии) и предзаполнением полей системы по имени файла
- Wizard со степпером на 9 шагов, авто-сохранением в localStorage, live-сводкой; шаг «Подбор» с анимацией 1.5 сек, SVG-схемой, BOM и итогом
- Страница ТКП A4 с реквизитами «Гидрострой-НН», объектом, BOM по системам, итогами, условиями, подписями, печатью через `window.print()` и экспортом в xlsx (точный формат архива)
- Каталог с 6 табами и фильтрами, справочник СП/ГОСТ (10 нормативов), настройки реквизитов компании с 4 табами
- Минорные правки в типах/моках: `Medium` перенесён в правильный модуль, добавлено обязательное `source: 'reservoir'` в FireData fire552
- Все 15 маршрутов возвращают 200, `tsc --noEmit` без ошибок, `verify-archive.ts` показывает 10/10
- статус: выполнено

## 20:50 — Ручная замена оборудования в спецификации (SkuPicker)

- Создан universal-combobox `SkuPicker.tsx` + CSS: поиск, бейджи совместимости, дельты цен, клавиатурная навигация, блок «Сейчас выбрано» + «Вернуть автоподбор»
- Создан wrapper `components/bom/BomReplaceButton.tsx`: кнопка ↻ возле строки BOM, авто-определение типа picker'а по `bomItem.group`, выборка альтернатив через `findAlternative*`, применение через store; + `OverridesBanner` для шапки BOM
- В `store/projects.ts` добавлены экшены `setSystemOverride` / `clearSystemOverrides` — после записи запускают `compute()` и кладут свежий BOM/itogo в стор
- Интеграция в `Wizard.tsx` (шаги «Подбор» и «Превью») + `Proposal.tsx` (no-print колонка с кнопкой ↻); draft синхронизируется со store по `updatedAt`
- Дымовой тест на sys-fire-552 показал: текущий насос = exact, альтернатива override меняет totalCost с 482 227 → 1 704 476 ₽, сброс возвращает автоподбор
- `tsc --noEmit` чист, `verify-archive.ts` 10/10
- статус: выполнено
